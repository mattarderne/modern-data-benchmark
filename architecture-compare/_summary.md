A modular benchmark comparing LLM performance across four data architectures (typed TypeScript, DBT/SQL, Drizzle ORM, and Cube semantic layer). Includes sandbox templates, a shared synthetic dataset, automated validators, and reports/charts in `artifacts/`. Status: **active**.
